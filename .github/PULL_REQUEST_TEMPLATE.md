## What changed

## Why

## Checks

- [ ] `gofmt -l .`, `go vet ./...`, `go test ./...`, `golangci-lint run ./...` are green
- [ ] JSON values and record formats are unchanged, or the change is described above
- [ ] A change to a review prompt or a store format links the trace or the eval report that shows the effect
